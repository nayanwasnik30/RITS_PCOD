{
  "name": "pulse-wellness-tracker",
  "version": "1.0.0",
  "description": "RIT PCOD Daily Wellness Tracker — Pulse",
  "private": true,
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "node scripts/build.js",
    "start": "node server.js",
    "preview": "npm run build && npm run start"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "html-minifier-terser": "^7.2.0",
    "clean-css-cli": "^5.6.3"
  },
  "dependencies": {
    "express": "^4.21.0",
    "compression": "^1.7.4"
  }
}
